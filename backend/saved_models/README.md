# Saved NLP models

Run from project root:
```bash
cd backend && python train_models.py
```

This generates `priority_model.pkl` and `scam_model.pkl` used by ml_engine for urgency and scam classification.
